- Build a nice looking html file that we will populate with commands
- Scrape the commands [x]
- Populate the web page with the scraped commands
- Transition into a chrome extension

- Features:
    - Allow for manual random select of new command
    - Add windows commands
    - Have the new commands be typed out letter by letter
